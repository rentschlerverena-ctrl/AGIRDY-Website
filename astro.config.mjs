import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://rentschlerverena-ctrl.github.io',
  base: '/main', // Ersetze dies durch den Namen deines Repositories
});
