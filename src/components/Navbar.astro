---
/* src/components/Navbar.astro */
const { lang = 'en' } = Astro.props; 
const homePath = lang === 'en' ? '/' : '/de';
const targetPath = lang === 'en' ? '/de' : '/';
---

<nav class="navbar">
  <div class="nav-container">
    <div class="logo">
      <a href={homePath}>
        <img src="/Logo_weiss.png" alt="AGIRDY Logo" class="logo-img" />
      </a>
    </div>

    <ul class="nav-links" id="nav-menu">
      <li><a href={`${homePath}#who-we-are`}>{lang === 'en' ? 'WHO WE ARE' : 'WER WIR SIND'}</a></li>
      <li><a href={`${homePath}#what-we-do`}>{lang === 'en' ? 'WHAT WE DO' : 'WAS WIR TUN'}</a></li>
      <li><a href={`${homePath}#vision`}>{lang === 'en' ? 'VISION & MISSION' : 'VISION & MISSION'}</a></li>
      
      <li class="dropdown">
        <div class="dropdown-trigger-wrapper">
          <a href={`${homePath}#product`}>{lang === 'en' ? 'PRODUCT' : 'PRODUKT'}</a>
          <span class="chevron">▾</span>
        </div>
        <ul class="dropdown-menu">
          <li><a href={`${homePath}#roadmap`}>ROADMAP</a></li>
        </ul>
      </li>

      <li class="dropdown">
        <div class="dropdown-trigger-wrapper">
          <a href="#">{lang === 'en' ? 'NEWS' : 'AKTUELLES'}</a>
          <span class="chevron">▾</span>
        </div>
        <ul class="dropdown-menu">
          <li><a href={`${homePath}blog`}>BLOG</a></li>
          <li><a href={`${homePath}podcast`}>PODCAST</a></li>
        </ul>
      </li>

      <li>
        <a href={`${homePath}#contact`} class="nav-cta">
          {lang === 'en' ? 'CONTACT' : 'KONTAKT'}
        </a>
      </li>
      
      <li class="lang-switch-container">
        <a href={targetPath} class="lang-switch">
          {lang === 'en' ? 'DE' : 'EN'}
        </a>
      </li>
    </ul>

    <div class="menu-toggle" id="mobile-menu">
      <span class="line"></span>
      <span class="line"></span>
      <span class="line"></span>
    </div>
  </div>
</nav>

<style>
  /* --- DESKTOP BASIS --- */
  .navbar {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 1000;
    background: rgba(10, 15, 20, 0.85);
    backdrop-filter: blur(15px);
    padding: 15px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .nav-container {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 5%;
  }

  .logo-img { height: 35px; width: auto; }

  .nav-links {
    display: flex;
    list-style: none;
    align-items: center;
    gap: 20px;
  }

  .nav-links a {
    text-decoration: none;
    color: white;
    font-family: 'Lato', sans-serif;
    font-weight: 700;
    font-size: 0.75rem;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    transition: 0.3s ease;
    opacity: 0.7;
  }

  .nav-cta {
    opacity: 1 !important;
    color: #adff2f !important;
    border: 1.5px solid #adff2f;
    padding: 10px 25px !important;
    border-radius: 50px;
    white-space: nowrap;
  }

  .lang-switch-container {
    margin-left: 10px;
    padding-left: 20px;
    border-left: 1px solid rgba(255, 255, 255, 0.15);
  }

  /* --- DESKTOP DROPDOWN --- */
  @media (min-width: 1025px) {
    .nav-links a:not(.nav-cta):hover { opacity: 1; color: #adff2f; }
    .dropdown { padding: 15px 0; margin: -15px 0; }
    .dropdown:hover .dropdown-menu { 
      opacity: 1; 
      visibility: visible; 
      transform: translateX(-50%) translateY(0); 
    }
    .dropdown:hover .chevron { transform: rotate(180deg); }
  }

  .dropdown { position: relative; }
  .dropdown-trigger-wrapper { display: flex; align-items: center; gap: 5px; cursor: pointer; }
  .chevron { color: white; font-size: 0.7rem; transition: transform 0.3s; opacity: 0.7; }

  .dropdown-menu {
    position: absolute;
    top: 100%; 
    left: 50%;
    transform: translateX(-50%) translateY(10px);
    background: #11181f;
    min-width: 180px;
    padding: 10px 0;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    opacity: 0;
    visibility: hidden;
    list-style: none;
  }

  .dropdown-menu a { padding: 12px 20px; display: block; font-size: 0.7rem; }

  /* --- MOBILE STYLES (ZENTRIERT & VOLLBILD) --- */
  .menu-toggle { display: none; flex-direction: column; gap: 6px; cursor: pointer; z-index: 1100; }
  .line { width: 28px; height: 2px; background: white; transition: 0.4s; }

  .menu-toggle.is-active .line:nth-child(1) { transform: translateY(8px) rotate(45deg); }
  .menu-toggle.is-active .line:nth-child(2) { opacity: 0; }
  .menu-toggle.is-active .line:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

  @media (max-width: 1024px) {
    .menu-toggle { display: flex; }

    .nav-links {
      position: fixed;
      right: -100%;
      top: 0;
      width: 100vw; /* Volle Breite */
      height: 100vh; /* Volle Höhe */
      background: #0a0f14;
      flex-direction: column;
      justify-content: center; /* Vertikale Zentrierung */
      align-items: center; /* Horizontale Zentrierung */
      padding: 0;
      margin: 0;
      transition: 0.5s cubic-bezier(0.77, 0.2, 0.05, 1.0);
      overflow-y: auto;
      z-index: 1050;
    }

    .nav-links.active { right: 0; }

    .nav-links li { 
      width: 100%; 
      text-align: center; 
      margin: 10px 0;
    }

    .nav-links a { 
      font-size: 1.6rem; 
      display: inline-block; 
      padding: 10px; 
      opacity: 1; 
    }

    /* Zentrierung für Dropdown Trigger */
    .dropdown-trigger-wrapper { 
      justify-content: center; 
      width: 100%;
    }

    .dropdown-menu {
      position: static;
      transform: none;
      opacity: 1;
      visibility: visible;
      display: none;
      background: rgba(255, 255, 255, 0.05);
      width: 100%;
      border: none;
    }
    .dropdown.open .dropdown-menu { display: block; }
    .dropdown-menu a { font-size: 1.2rem; opacity: 0.6; }

    /* CTA & Sprachschalter zentriert */
    .nav-cta {
      margin: 15px auto;
      display: inline-block;
      font-size: 1.6rem !important;
    }

    .lang-switch-container {
      border: none;
      margin: 20px 0 0 0;
      padding: 0;
      width: 100%;
    }
  }
</style>

<script>
  const menuToggle = document.getElementById('mobile-menu');
  const navMenu = document.getElementById('nav-menu');
  const dropdowns = document.querySelectorAll('.dropdown');
  const body = document.body;

  menuToggle.addEventListener('click', () => {
    const isActive = menuToggle.classList.toggle('is-active');
    navMenu.classList.toggle('active');
    
    // Body Scroll Lock
    body.style.overflow = isActive ? 'hidden' : '';
  });

  // Mobile Accordion Logic
  dropdowns.forEach(dropdown => {
    const trigger = dropdown.querySelector('.dropdown-trigger-wrapper');
    trigger.addEventListener('click', (e) => {
      if (window.innerWidth <= 1024) {
        // Verhindert das Schließen des Menüs beim Klick auf Dropdown
        e.stopPropagation(); 
        dropdown.classList.toggle('open');
      }
    });
  });

  // Schließen bei Link-Klick
  document.querySelectorAll('.nav-links a').forEach(link => {
    link.addEventListener('click', (e) => {
      // Nur schließen, wenn es kein Dropdown-Öffner ist
      if (window.innerWidth <= 1024 && link.closest('.dropdown-trigger-wrapper')) return;
      
      menuToggle.classList.remove('is-active');
      navMenu.classList.remove('active');
      body.style.overflow = '';
    });
  });
</script>