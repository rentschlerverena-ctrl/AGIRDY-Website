---
/* src/components/WhoWeAre.astro */
const { teamData } = Astro.props; 

if (!teamData) {
  console.error("WhoWeAre: Keine teamData empfangen!");
}
---

<section class="who-we-are" id="who-we-are">
  <div class="container">
    <div class="title-row">
      <h2 class="section-title">{teamData.title}</h2>
      <p class="section-title-sub">{teamData.subtitle}</p>
    </div>
    
    <div class="team-grid">
      {teamData.members.map(member => (
        <div class="card centered">
          <div class="profile-image-container">
            <div class="image-ring">
              <img src={member.image} alt={member.name} class="profile-img" />
            </div>
          </div>
          
          <div class="card-header">
            <h1 class="card-title">{member.name}</h1>
            <p class="card-subtitle">{member.role}</p>
          </div>
          
          <p class="card-desc bio-content" set:html={member.bio} />

          {member.linkedin && (
            <a href={member.linkedin} target="_blank" rel="noopener noreferrer" class="social-link" aria-label={`LinkedIn Profil von ${member.name}`}>
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 448 512" class="social-icon">
                <path d="M416 32H31.9C14.3 32 0 46.5 0 64.3v383.4C0 465.5 14.3 480 31.9 480H416c17.6 0 32-14.5 32-32.3V64.3c0-17.8-14.4-32.3-32-32.3zM135.4 416H69V202.2h66.5V416zm-33.2-243c-21.3 0-38.5-17.3-38.5-38.5S80.9 96 102.2 96c21.2 0 38.5 17.3 38.5 38.5 0 21.3-17.2 38.5-38.5 38.5zm282.1 243h-66.4V312c0-24.8-.5-56.7-34.5-56.7-34.6 0-39.9 27-39.9 54.9V416h-66.4V202.2h63.7v29.2h.9c8.9-16.8 30.6-34.5 62.9-34.5 67.2 0 79.7 44.3 79.7 101.9V416z"/>
              </svg>
            </a>
          )}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  /* DEIN WICHTIGER PART FÜR DIE SICHTBARKEIT */
  .who-we-are {
    position: relative;
    z-index: 10;
    min-height: 100vh;
    padding: 120px 0;
    background: transparent;
    width: 100%;
  }

  .container {
    max-width: 100%;
    margin: 0 auto;
  }

  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 400px));
    gap: 3rem;
    padding: 0 5%;
    justify-content: center;
  }

  /* Team-Spezifisch: Profilbilder */
  .image-ring {
    width: 130px;
    height: 130px;
    border-radius: 50%;
    border: 2px solid var(--accent);
    margin: 0 auto 2rem;
    overflow: hidden;
  }

  .profile-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: grayscale(100%);
    transition: filter 0.3s ease;
  }

  .card:hover .profile-img {
    filter: grayscale(0%);
  }

  /* Accent im Fliesstext (Bio) */
  .bio-content :global(strong) {
    color: var(--accent);
    font-weight: normal;
    letter-spacing: 0.5px;
  }

  /* LinkedIn Icons Styling */
  .social-link {
    display: flex;
    justify-content: center;
    margin-top: 2rem;
    transition: transform 0.2s ease;
  }

  .social-icon {
    width: 20px;
    height: 20px;
    fill: var(--text);
    opacity: 0.4;
    transition: all 0.3s ease;
  }

  .social-link:hover .social-icon {
    opacity: 1;
    fill: var(--accent);
  }
</style>